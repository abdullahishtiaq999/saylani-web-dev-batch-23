// Linear Search Algorithm ( with sorted and unsorted data )

const array = [10, 20, 30, 40, 50, 60, 70, 80, 90];

const target = 70;

// function findNum(arr, target) {
//     for (let i = 0; i < arr.length; i++) {
//         if (arr[i] === target) {
//             return i;
//         };
//     };

//     return -1;
// };

// const result = findNum(array, target);

// if (result !== -1) {
//     console.log(`Target Number Found at ${result}`);
// } else {
//     console.log("Target Number is Not Found");
// };

// Linear Search Algorithm with Unsorted Data